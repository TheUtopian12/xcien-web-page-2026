
<section class="py-32 bg-black relative overflow-hidden">
  <div class="container mx-auto px-4 relative z-10">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold max-w-5xl mx-auto leading-tight text-white">
        Solución a la <span class="text-primary italic">medida</span> para una <span class="text-primary italic">micro o pequeña empresa</span> que requiere de servicio de internet de calidad a un precio competitivo. Servicio con <span class="text-primary italic">mínima sobresuscripción</span>, ideal para la operación crítica.
      </h2>
      
      <div class="mt-12 flex flex-col items-center gap-4">
        <div class="w-12 h-1.5 bg-primary/20 rounded-full flex items-center justify-center">
          <div class="w-3 h-3 bg-primary rounded-full shadow-[0_0_10px_rgba(163,230,53,0.8)]"></div>
        </div>
        <span class="text-primary font-black italic uppercase tracking-[0.3em] text-sm">
          Comparativa de Servicio
        </span>
      </div>
    </div>

    <div class="flex flex-col lg:flex-row items-center justify-center gap-12 lg:gap-8 max-w-6xl mx-auto mt-20">
      
      
      <div class="relative group w-full max-w-md">
        
        <div class="absolute -left-6 top-1/2 -translate-y-1/2 flex flex-col gap-4 z-20">
          {[1, 2, 3].map(() => (
            <div class="w-10 h-10 rounded-full bg-[#a3e635] border-2 border-black flex items-center justify-center text-black shadow-lg">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            </div>
          ))}
        </div>
        
        <div class="bg-[#a3e635] rounded-[2.5rem] p-10 h-48 flex flex-col items-center justify-center text-center shadow-[0_0_50px_rgba(163,230,53,0.3)] transition-transform duration-500 group-hover:scale-105 border-4 border-white/20">
          <h3 class="text-3xl md:text-4xl font-black text-black leading-none mb-2">Enlace xcien</h3>
          <p class="text-black/80 text-xl font-bold">Mínima sobresuscripción</p>
        </div>
      </div>

      
      <div class="relative group w-full max-w-md">
        
        <div class="absolute -left-6 top-1/2 -translate-y-1/2 flex flex-col gap-2 z-20">
          {[
            {bg: 'bg-blue-400'},
            {bg: 'bg-rose-400'},
            {bg: 'bg-blue-500'},
            {bg: 'bg-orange-400'},
            {bg: 'bg-blue-600'}
          ].map((icon) => (
            <div class={`w-8 h-8 rounded-full ${icon.bg} border-2 border-black flex items-center justify-center text-white shadow-lg -mt-1`}>
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            </div>
          ))}
        </div>
        
        <div class="bg-[#e11d48]/80 backdrop-blur-sm rounded-[2.5rem] p-10 h-48 flex flex-col items-center justify-center text-center shadow-[0_0_50px_rgba(225,29,72,0.2)] transition-transform duration-500 group-hover:scale-105 border-4 border-white/10 ml-4">
          <h3 class="text-3xl md:text-4xl font-black text-white leading-none mb-2">Otros proveedores</h3>
          <p class="text-white/70 text-xl font-bold">Sobresuscripción</p>
        </div>
      </div>

    </div>
  </div>
</section>

    </div>
  </div>
</section>

<style>
  
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>
