<section class="pt-12 pb-10 relative overflow-hidden">
  <div class="container mx-auto px-4 relative z-20">
    <h2
      class="text-2xl md:text-3xl font-black text-center mb-12 text-white/40 tracking-tight uppercase italic"
    >
      Alianzas Estratégicas
    </h2>

    <div class="relative w-full overflow-hidden py-16 group">
      <!-- Gradient masks for smooth edges -->

      <div
        class="flex items-center gap-24 animate-marquee whitespace-nowrap transition-all duration-700"
      >
        {
          [1, 2].map(() => (
            <div class="flex items-center gap-24 flex-shrink-0">
              {Array.from({ length: 18 }).map((_, i) => (
                <a href="/alianzas" class="hover:scale-110 transition-transform duration-500 block">
                  <img
                    src={`/logos/logos xcien-${(i + 1).toString().padStart(2, '0')}.png`}
                    alt={`Partner ${i + 1}`}
                    class="h-28 md:h-36 object-contain grayscale opacity-100 brightness-[1.8] hover:grayscale-0 hover:brightness-100 transition-all duration-500"
                  />
                </a>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  .animate-marquee {
    display: flex;
    width: max-content;
    animation: marquee 60s linear infinite;
    will-change: transform;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
  }

  @keyframes marquee {
    0% {
      transform: translate3d(0, 0, 0);
    }
    100% {
      transform: translate3d(-50%, 0, 0);
    }
  }
</style>
